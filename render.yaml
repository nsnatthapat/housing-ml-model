services:
  - type: web
    name: housing-ml-model
    runtime: python
    buildCommand: pip install -r requirements.txt && python train.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
